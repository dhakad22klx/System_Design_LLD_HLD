1. Go does not support traditional type inheritance like C++ (a child class inheriting from a base class). 
Instead, Go promotes composition over inheritance, achieving similar functionality through struct embedding and interfaces, 
which allow struct fields and methods to be promoted to outer structs.